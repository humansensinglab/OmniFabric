# Paper PDF

The final PDF is included in the repository:

`assets/paper/omnifabric.pdf`

`js/data.js` links to it directly via `links.paper: "assets/paper/omnifabric.pdf"`, so the page is fully self-contained and no longer depends on an external URL.
